# Minimal Prometheus scrape config for a keldron-agent on the same host.
# For Docker Compose, use prometheus.docker.yml (scrapes host.docker.internal:9100).

global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'keldron-agent'
    static_configs:
      - targets: ['localhost:9100']
